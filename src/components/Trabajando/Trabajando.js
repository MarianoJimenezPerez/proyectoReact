import React from 'react'

export const Trabajando = (props) => {

    return (
        <section >
            <h2 className="t-center mt-2">Estamos trabajando en esta sección, visita nuestra tienda y vuelve pronto</h2>
        </section>
    )
}